pipelines:
  default:
    - step:
        name: Test
        services:
          - docker
        script:
          - docker build .
          - docker exec -it node yarn test
        caches:
          - docker
          - node