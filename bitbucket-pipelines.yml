pipelines:
  default:
    - step:
        name: Test
        services:
          - docker
        script:
          - docker build .Dockerfile
          - yarn
          - yarn test
        caches:
          - docker
          - node