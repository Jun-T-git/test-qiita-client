pipelines:
  default:
    - step:
        name: Test
        services:
          - docker
        script:
          - docker build . -t next
          - docker run next yarn test
        caches:
          - docker
          - node
definitions:
  services:
    docker:
      memory: 2048