pipelines:
  default:
    - step:
        name: Test
        services:
          - docker
        script:
          - docker build .
          - bash yarn test
        caches:
          - docker
          - node